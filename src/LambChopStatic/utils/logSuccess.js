export default (folderPath) => {
  console.info(`
    ===================================================================
      Successfully generating static files without errors.
      Please check generated files at ${folderPath}
    ===================================================================
  `);
};
